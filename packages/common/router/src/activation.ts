
import {RouteState} from './shared';
import {RouteConfig} from './config';


/**
 * Activate a single route. This will be called, once for each RouteState being activated.
 */
function activateRoute(config: RouteConfig, state: RouteState) {
  // Need the Component type to create & somewhere to attach it

  // Need to create the Component and attach
}

/**
 * Get the applicable outlet.
 */
function getRouterOutlet(state: RouteState) {

}